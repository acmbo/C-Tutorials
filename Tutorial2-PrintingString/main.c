#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    /* Main Function
    This is a commentsection across multiple rows
    */
    int age = 40; // Whole numbers
    double gpa = 3.7; // Decimal numbers
    char grade = 'A'; // Single Char
    char phrase[] = "Test"; // Array of Charakters, Read Only!
    printf("%f", pow(2,3));
    printf("%f", floor(36.777));
    printf("%f", ceil(35.77));
    return 0;
}
